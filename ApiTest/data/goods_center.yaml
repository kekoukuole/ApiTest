get_banner:
  #接口的基本信息
  - request_info:
      case_title: banner接口
      url:  /banners/
      method: GET
      headers:
        Content-Type: application/json
    case_info:
      #接口请求参数
      - params:
        validate:
          - length: [ "$..id", 1 ]